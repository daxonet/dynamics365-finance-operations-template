trigger: none

name: $(Date:yy.MM.dd)$(Rev:.r)

stages: 
- template: build-xpp.yml
# below for add a license file to the package for each model with licensing
#  parameters:
#    modelsWithLicense: ['model1','model2']